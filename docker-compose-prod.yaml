version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.prod
    volumes:
      - ./db:${DB_PATH}
    ports: 
      - 15352:${PORT}
    environment:
      - PORT=${PORT}
      - VK_KEY=${VK_KEY}
      - APP_ID=${APP_ID}
      - DB_PATH=${DB_PATH}
    tty: true
